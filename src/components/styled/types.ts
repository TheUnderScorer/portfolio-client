export interface HomeSectionProps {
    centered?: boolean;
    odd?: boolean;
}

export interface SectionTitleProps {
    underlined?: boolean;
}

export interface ButtonProps {
    flat?: boolean;
    withIcon?: boolean;
    ripple?: boolean;
}
