export interface LoaderContainerProps {
    asOverlay?: boolean;
    width: string | number;
    height: string | number;
    active?: boolean;
}

export interface SvgLoaderProps {
    width: string | number;
    height: string | number;
}
